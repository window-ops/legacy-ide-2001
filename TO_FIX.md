Let's make the command prompt icon just C:\ because the > doesn't fit, the Dr. Watson debugger does not seem to work when patching, there is also this error (Uncaught TypeError: can't access property "contains", document.getElementById(...) is null), downloadable font: download failed (font-family: "Cantarell" style:normal weight:700 stretch:100 src index:0): status=2152857618 source: file://.../legacy-ide/fonts/cantarell/Cantarell-Bold.ttf. And also, when the IDE is minimized, show the notification like the one where you cannot log off with a baloon instead of a toast (because when the IDE is minimized, the toast does not display)